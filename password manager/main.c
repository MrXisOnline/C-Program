#include "dependencies.h"
#include <string.h>


int main()
{
	// char username[80], passwd[80];
	// int len_str;
    // printf("Enter Username: "); scanf("%s", &username);
    // printf("Enter Password: "); scanf("%s", &passwd);
	// // // calculates length of string
	// len_str = strlen(passwd);
	
	// // // // to exclude '\0' character
	// len_str -= 1;
    // char *enc_pass = base64Encoder(passwd, len_str);
	// Cred data;
    // strcpy(data.user, username);
	// strcpy(data.passwd, enc_pass);
	// data.pass_len = len_str;
    // printf("\n%s, %s, %d\n", data.user, data.passwd, data.pass_len);
    // add_crediential(data, "02");
	show_all_credientials();
	// char sno[] = "02";
	// remove_credentials(sno);
	// show_all_credientials();
	// printf("Input string is : %s\n", input_str);
	// printf("Encoded string is : %s\n", enc);
    // // calculates length of string
	// printf("Decoded string is : %s\n", base64Decoder(enc, len_str));
	return 0;
}
